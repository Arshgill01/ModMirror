# Capability Research Protocol

Classify capabilities as `verified_runtime`, `verified_static`, `type_only`, `unsupported`, `failed_runtime`, or `deferred`. Record commands, subreddit, target IDs, action attempted, result, and errors. UI must not present unverified capabilities as enabled.
