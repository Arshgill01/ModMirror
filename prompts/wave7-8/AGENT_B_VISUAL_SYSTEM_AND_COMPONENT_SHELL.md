# AGENT B — Visual System + Component Shell

Goal: create the UI foundation that prevents Codex slop.

Tasks:
1. Implement CSS design tokens.
2. Build reusable components if project structure allows: AppShell, InlineLaunchCard, Navigation, MetricCard, StatusBadge, ActionCard, EmptyState, SectionHeader, PrimaryActionBar, InboxCard, MarkdownPanel.
3. Replace ad-hoc styling with consistent classes.
4. Ensure responsive desktop/tablet/mobile layout.
5. Avoid large UI dependencies unless already present or strongly justified.

Acceptance: app compiles, tokens exist, reusable components are used, no functionality regression.
